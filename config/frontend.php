<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Frontend Path (путь к папке фронтенда)
    |--------------------------------------------------------------------------
    |
    | Относительный путь от корня API (base_path) к папке фронтенд-приложения.
    | Например: ../admin.skateandsnow.ru или ../my-admin-frontend
    |
    | ВАЖНО: Задаётся ТОЛЬКО в .env (FRONTEND_PATH). В коде нет запасного
    | значения — при смене проекта нужно обновить .env.
    |
    */

    'path' => env('FRONTEND_PATH'),

];
